# 外部导入文件 - 导入测试 (MEM19/MEM20)

<!-- MEM-IMPORT-MARKER: 此文件通过 @pathToImport 被导入到 AGENTS.md -->

## 导入验证

- [MEM19-IMPORTED] 此文件通过相对路径 `@docs/memory-test-import.md` 被导入
- 如果此内容出现在模型上下文中，说明 @pathToImport 语法工作正常

## 导入的编码规范

- 所有 API 接口需要统一前缀 `/api/v1/`
- 响应格式统一使用 `{ code, data, message }` 结构
