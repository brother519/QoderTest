export interface Position {
  x: number;
  y: number;
}

export interface GridPosition {
  x: number;
  y: number;
}

export interface TankConfig {
  speed: number;
  health: number;
  bulletPower: number;
  maxBullets: number;
}
